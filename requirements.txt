# requirements.txt - pinned, reproducible runtime dependencies for module-ttt.
#
# Generated from a clean install of pyproject.toml's [tool.poetry.dependencies]
# plus two dependencies the code actually imports but pyproject.toml never
# declares (see the "undeclared in pyproject.toml" section below) - installing
# only what's in pyproject.toml is NOT enough to run this app; app/main.py
# will fail to import without minio and app/services/indexer.py will fail
# without tqdm.
#
# Install with:
#   pip install -r requirements.txt
#
# The three talkingdb-* packages and package-content-elementizer are private
# GitHub repos pinned to exact commits - installing them requires GitHub
# access (SSH key or HTTPS credentials) that can reach the TalkingDB org.

# --- web framework -----------------------------------------------------
fastapi==0.125.0
starlette==0.50.0
uvicorn[standard]==0.38.0
uvloop==0.22.1
httptools==0.8.0
watchfiles==1.2.0
websockets==17.0.1
python-multipart==0.0.20
annotated-doc==0.0.5

# --- config / env -------------------------------------------------------
python-dotenv==1.2.2
dotenv==0.9.9

# --- data validation ------------------------------------------------------
pydantic==2.13.4
pydantic_core==2.46.4
email-validator==2.3.0
typing_extensions==4.16.0
typing-inspection==0.4.2

# --- graph / NLP ----------------------------------------------------------
# spaCy and its compiled dependencies (blis, thinc, murmurhash, etc.) publish
# different wheel sets per platform/Python version - hard-pinning the exact
# patch version that resolved on one machine can make installation fail on
# another (e.g. spacy==3.8.15 exists on Linux but not on some macOS/Python
# combos). Range-pinned here to match pyproject.toml's own "^3.8.11", and
# let pip pick whatever compatible build exists for your platform.
networkx>=3.6.1,<4.0.0
spacy>=3.8.11,<4.0.0
spacy-legacy>=3.0.12,<4.0.0
spacy-loggers>=1.0.5,<2.0.0
thinc>=8.3.0,<9.0.0
blis>=1.0.0,<2.0.0
catalogue>=2.0.10,<3.0.0
confection>=0.1.0,<2.0.0
cymem>=2.0.13,<3.0.0
murmurhash>=1.0.15,<2.0.0
preshed>=3.0.13,<4.0.0
srsly>=2.5.0,<3.0.0
wasabi>=1.1.3,<2.0.0
weasel>=0.1.0,<1.1.0
cloudpathlib>=0.20.0,<1.0.0
smart_open>=7.0.0,<9.0.0
numpy>=2.0.0,<3.0.0

# spaCy's "en_core_web_md" language model is not a normal PyPI package and
# cannot be pinned as a plain requirements.txt line. Install it separately
# after the packages above, e.g.:
#   python -m spacy download en_core_web_md
# app.services.package_text_tokenizer.TextTokenizer loads this model by name
# at runtime - the app will raise on first use without it.

# --- talkingdb private packages (pinned commits; require GitHub access) --
git+https://github.com/TalkingDB/base-tdb-models.git@5db6dd4375c06f00e6819d242f8668d2590e6c04#egg=talkingdb-models
git+https://github.com/TalkingDB/base-tdb-clients.git@ba0794a0395ce840b757c80ea08e75d68dc1d2be#egg=talkingdb-clients
git+https://github.com/TalkingDB/base-tdb-helpers.git@f601bb365a545415121095a55c9aed5618dfaaba#egg=talkingdb-helpers
git+https://github.com/TalkingDB/package-content-elementizer.git@8443f0995b7027c60750e901f0a9de0daacbcc1a#egg=package-content-elementizer

# --- document parsing (pulled in via package-content-elementizer) --------
python-docx>=1.2.0,<2.0.0
pymupdf>=1.24.0,<2.0.0
pdf2docx>=0.5.6,<1.0.0
opencv-python-headless>=4.9.0,<6.0.0
lxml>=5.0.0,<7.0.0
smart-slugify>=0.1.0,<1.0.0

# --- auth -------------------------------------------------------------
python-jose==3.5.0
argon2-cffi>=23.1.0,<26.0.0
argon2-cffi-bindings>=21.2.0,<26.0.0
cryptography>=42.0.0,<51.0.0
bcrypt>=4.0.0,<6.0.0
ecdsa==0.19.2
pyasn1==0.6.4
rsa==4.9.1
pycparser>=2.21,<3.0.0
cffi>=1.16.0,<3.0.0
pycryptodome>=3.20.0,<4.0.0

# --- misc runtime ---------------------------------------------------------
debugpy==1.8.21
tqdm==4.70.0                 # used by app/services/indexer.py - UNDECLARED in pyproject.toml
minio==7.2.20                # used by app/api/documents.py    - UNDECLARED in pyproject.toml
requests==2.34.2
tenacity==9.1.4
dnspython==2.8.0
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0
anyio==4.14.2
h11==0.16.0
httpcore==1.0.9
click==8.4.2
rich==15.0.0
Pygments==2.20.0
markdown-it-py==4.2.0
mdurl==0.1.2
Jinja2==3.1.6
MarkupSafe==3.0.3
PyYAML==6.0.3
packaging==26.3
setuptools==84.0.0
six==1.17.0
wrapt==2.3.0
fire==0.7.1
termcolor==3.3.0
typer==0.27.1
shellingham==1.5.4
fonttools==4.63.0
